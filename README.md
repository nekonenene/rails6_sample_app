# README

```sh
rails new app --skip-git --skip-action-mailbox --skip-action-text --skip-spring --skip-turbolinks --skip-bundle --skip-webpack-install --skip-javascript
```

```sh
gibo dump Rails macOS Windows VisualStudioCode > .gitignore
```

```sh
bundle install --path=vendor/bundle -j 4
```
